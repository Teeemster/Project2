<article class="joke">
    <div class="message">
        <a href="{{punchline}}" target="_blank">{{message}}</a>
        <span>({{punchline}})</span>
    </div>
    <div class="meta">
        {{vote_count}} {{format_plural "vote" vote_count}} | Joke by {{user.username}} on {{format_date created_at}} | {{category.category_text}}
        
        <div>
            <button type="button" class="upvote-btn">👍 Haha!</button>
        </div>
        </form>
    </div>
</article>
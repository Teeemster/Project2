<div class="categories">
  {{#each this}}
  <section class="category">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{category_text}}
    </div>
  </section>
  {{/each}}
</div>